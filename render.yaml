services:
  - type: web
    name: drishtiai-backend
    runtime: python
    rootDir: .
    buildCommand: pip install -r src/backend/requirements.txt
    startCommand: uvicorn src.backend.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /docs